<Window x:Class="SmartWords.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:SmartWords.ViewModels"
        Title="Smart Words" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#FF1E1E1E"
        Foreground="White"
        FontFamily="Segoe UI">
    
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    
    <Window.Resources>
        <!-- Ресурс для тени -->
        <DropShadowEffect x:Key="DropShadowEffect" ShadowDepth="0" BlurRadius="10" Color="#40000000"/>

        <!-- Стили для кнопок -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect" Value="{StaticResource DropShadowEffect}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF444444"/>
                    <Setter Property="Foreground" Value="#FFDDDDDD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стили для текстовых блоков -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="10"/>
        </Style>

        <!-- Стиль для TabControl -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="#FF1E1E1E"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <!-- Стиль для TabItem -->
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="#FF2D2D2D"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="1" 
                            CornerRadius="5,5,0,0" 
                            Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="ContentSite"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"
                                          ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FF444444"/>
                            </Trigger>
                           
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Анимация для появления элементов -->
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.5"/>
        </Storyboard>


        <!-- Стиль для Grid с анимацией -->
        <Style TargetType="Grid">
            <Style.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard Storyboard="{StaticResource FadeInAnimation}"/>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <TabControl SelectedIndex="1">
        <TabControl.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard Storyboard="{StaticResource FadeInAnimation}"/>
            </EventTrigger>
        </TabControl.Triggers>
        
        <TabItem Header="Быстрое изучение">
            <Grid Cursor="">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Основной контент -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard Storyboard="{StaticResource FadeInAnimation}"/>
                </EventTrigger>
            </Grid.Triggers>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!--блок Изучение рандомных слов -->
            <Border Grid.Column="0" Background="#FF2D2D2D" CornerRadius="10" Padding="10"
                    Effect="{StaticResource DropShadowEffect}">
                <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                    <TextBlock Text="Смотри и запоминай" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="20" FontWeight="Bold" Height="30"/>
                            <TextBlock Text="{Binding CurrentWord.En}" Grid.Row="0" FontSize="35" Width="200" Height="50" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <TextBlock Text="{Binding CurrentWord.Ru}" Grid.Row="1" FontSize="35" Width="auto" Height="100" VerticalAlignment="Top" TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding CurrentWord.Tr}" Grid.Row="0" FontSize="24" Width="200" Height="35" VerticalAlignment="Bottom" HorizontalAlignment="Left" Padding="10,0" Margin="0,40"/>
                    <Button x:Name="NextWordButton" Content="Дальше" Grid.Row="1" VerticalAlignment="Bottom" Command="{Binding NextWordCommand}"/>
                </Grid>
            </Border>
            <!-- График успехов -->
            <Border Grid.Column="1" Background="#FF2D2D2D" CornerRadius="10" Padding="10" Margin="20,0,0,0"
                    Effect="{StaticResource DropShadowEffect}">
                <Grid>
                     <TextBlock Text="График успехов:" FontSize="20" FontWeight="Bold"/>
                     <Button Content="Посмотреть за месяц" VerticalAlignment="Bottom"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Дополнительный контент -->
        <Border Grid.Row="2" Background="#FF2D2D2D" CornerRadius="10" Padding="10" Margin="20"
                Effect="{StaticResource DropShadowEffect}">
            <Border.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard Storyboard="{StaticResource FadeInAnimation}"/>
                </EventTrigger>
            </Border.Triggers>
        </Border>
    </Grid>
        </TabItem>
        <TabItem Header="Тест" Width="100">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{Binding CurrentWord.En}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="60" Width="auto" Height="auto" Cursor=""/>
                <TextBlock Text="{Binding CurrentWord.Tr}" VerticalAlignment="Bottom" HorizontalAlignment="Center" FontSize="40" Width="auto" Height="auto" Cursor=""/>

                <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Cursor="">
                    <Button Content="Привет" Width="450" Height="40" Margin="0,5"/>
                    <Button Content="Пока" Width="450" Height="40" Margin="0,5"/>
                    <Button Content="Спасибо" Width="450" Height="40" Margin="0,5"/>
                </StackPanel>
            </Grid>
        </TabItem>
    </TabControl>
</Window>